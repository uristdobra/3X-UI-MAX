# 3X-UI PRO INSTALLER v2.0 - ПОЛНОЕ РУКОВОДСТВО

## 📦 Содержание пакета

Этот пакет содержит полный набор инструментов для развёртывания и управления 3x-ui (Xray) на Ubuntu 22.04 с поддержкой 7 протоколов:

### 📄 Файлы в пакете:

1. **x-ui-pro-installer.sh** - Главный скрипт установки (требует root)
2. **inbound-config.sh** - Библиотека функций для создания инбаундов
3. **maintenance.sh** - Инструмент для мониторинга и резервного копирования
4. **README.md** - Полная документация
5. **client-config-examples.md** - Примеры конфигураций для клиентов
6. **nginx-integration.md** - Подробное руководство по Nginx
7. **SETUP_GUIDE.txt** - Этот файл

---

## 🚀 Быстрый старт (5 минут)

### Шаг 1: Скачайте скрипты

```bash
# Создайте директорию
mkdir -p ~/3xui-installer
cd ~/3xui-installer

# Скачайте все файлы
wget https://your-server.com/x-ui-pro-installer.sh
wget https://your-server.com/inbound-config.sh
wget https://your-server.com/maintenance.sh

# Дайте права на выполнение
chmod +x *.sh
```

### Шаг 2: Запустите установку

```bash
sudo ./x-ui-pro-installer.sh
```

### Шаг 3: Следуйте интерактивному диалогу

**Примерные вопросы:**

```
═════════════════════════════════════════════════
Домен панели: panel.example.com
REALITY SNI: www.microsoft.com
Email LE: admin@example.com
Admin Password: ***** (не забудьте!)

Выберите протоколы:
1,2,3,4 (VLESS REALITY + WebSocket по умолчанию)
```

### Шаг 4: Дождитесь завершения

Установка займёт **5-15 минут** в зависимости от вашего сервера.

---

## 📋 Поддерживаемые протоколы

После установки будут доступны следующие протоколы:

### 1️⃣ VLESS + REALITY TCP (10001)
- **Назначение:** Основной stealth-трафик, максимальная скрытность
- **Безопасность:** REALITY
- **Транспорт:** TCP
- **Flow:** xtls-rprx-vision
- **Клиенты:** Sing-box, v2rayN, Clash Meta

### 2️⃣ VLESS + REALITY gRPC (10002)
- **Назначение:** Обход DPI через gRPC
- **Безопасность:** REALITY
- **Транспорт:** gRPC
- **Клиенты:** Sing-box, Clash Meta

### 3️⃣ VLESS + REALITY XHTTP (10003)
- **Назначение:** Маскировка под HTTP/1.1 upgrade
- **Безопасность:** REALITY
- **Транспорт:** HttpUpgrade/XHTTP
- **Клиенты:** Sing-box, Clash Meta

### 4️⃣ VLESS + WebSocket + TLS (10004)
- **Назначение:** CDN-friendly, совместимо с Cloudflare
- **Безопасность:** TLS
- **Транспорт:** WebSocket
- **Путь:** /ws
- **Клиенты:** Все современные клиенты

### 5️⃣ VMess + TCP (10005)
- **Назначение:** Обратная совместимость со старыми клиентами
- **Безопасность:**없음 (None)
- **Транспорт:** TCP
- **Клиенты:** v2rayN, v2rayA, v2rayNG

### 6️⃣ Trojan + REALITY TCP (10006)
- **Назначение:** Альтернативный stealth-протокол
- **Безопасность:** REALITY
- **Транспорт:** TCP
- **Клиенты:** Sing-box, Clash Meta, v2rayN

### 7️⃣ ShadowSocks + TCP (10007)
- **Назначение:** Лёгкий протокол, простая настройка
- **Метод:** aes-256-gcm
- **Транспорт:** TCP
- **Клиенты:** SS, Clash, SagerNet, Shadowrocket

---

## 🎯 Что происходит во время установки

### Раздел 1: Подготовка сервера
```
✓ Обновление Ubuntu
✓ Установка утилит (curl, wget, jq, net-tools, etc.)
✓ Проверка портов (22, 80, 443)
✓ Отключение UFW (временно)
✓ Оптимизация sysctl для TCP/BBR
✓ Очистка неиспользуемых пакетов
```

### Раздел 2: Интерактивный ввод
```
✓ Ввод домена панели
✓ Ввод SNI для REALITY
✓ Email для Let's Encrypt
✓ Пароль администратора
```

### Раздел 3: Выбор протоколов
```
✓ Интерактивный выбор из 7 протоколов
✓ По умолчанию выбраны: VLESS REALITY (все варианты) + WebSocket
```

### Раздел 4: Установка зависимостей
```
✓ Docker + Docker Compose
✓ Nginx (для reverse proxy)
✓ Certbot (для SSL)
```

### Раздел 5: Установка 3x-ui
```
✓ Загрузка образа из GitHub
✓ Создание docker-compose.yml
✓ Запуск контейнера
```

### Раздел 6: Конфигурирование Nginx
```
✓ Получение SSL сертификата через Let's Encrypt
✓ Настройка reverse proxy для всех транспортов
✓ WebSocket (/ws), gRPC (/xray), XHTTP (/xhttp) endpoints
```

### Раздел 7: Создание инбаундов
```
✓ Генерация REALITY ключей
✓ Создание всех выбранных протоколов через REST API
✓ Сохранение UUID и паролей
```

### Раздел 8: Финальные проверки
```
✓ Проверка контейнеров
✓ Проверка Nginx и портов
✓ Проверка SSL сертификата
```

---

## 📁 Структура после установки

```
/opt/3xui/
├── docker-compose.yml              # Docker конфиг
├── db/
│   └── x-ui.db                    # БД панели 3x-ui
├── certs/
│   └── (SSL сертификаты)
├── backups/                       # Директория резервных копий
├── installation-config.txt        # Вся информация об установке
├── credentials.txt                # Пароли и ключи
├── x-ui/                          # Клон репозитория
└── ...

/etc/nginx/
├── sites-available/3xui-panel     # Основная конфиг
├── sites-enabled/3xui-panel       # Активный симлинк
└── ...

/etc/letsencrypt/
└── live/panel.example.com/
    ├── fullchain.pem
    ├── privkey.pem
    └── ...

~/.ssh/                            # SSH ключи (резервные копии)
```

---

## 🔐 Учётные данные и ключи

После установки все параметры сохраняются в **двух местах**:

### 1. `/opt/3xui/installation-config.txt`
```
Домен панели: panel.example.com
REALITY SNI: www.microsoft.com
Admin Password: ****

Установленные протоколы:
✓ VLESS + REALITY TCP (порт 10001)
✓ VLESS + REALITY gRPC (порт 10002)
✓ VLESS + REALITY XHTTP (порт 10003)
✓ VLESS + WebSocket + TLS (порт 10004)
✓ VMess + TCP (порт 10005)
✓ Trojan + REALITY TCP (порт 10006)
✓ ShadowSocks + TCP (порт 10007)

REALITY ключи:
Private Key: ***
Public Key: ***

Trojan Password: ***
ShadowSocks Password: ***
```

### 2. `/opt/3xui/credentials.txt`
Просто и ясно:
```
UUID: ***
Trojan Pass: ***
SS Password: ***
SS Method: aes-256-gcm
```

---

## 🌐 Доступ к панели

### URL панели:
```
https://panel.example.com
```

### Логин:
```
Пользователь: admin
Пароль: [Тот, что вы ввели при установке]
```

### REST API:
```
http://127.0.0.1:54321/api/...
```

### Порты внутреннего использования:
- **8080** - Web панель (через Nginx)
- **54321** - REST API (только localhost)
- **10001-10007** - Инбаунды Xray

---

## 🛠️ Основные команды

### Просмотр статуса:
```bash
# Статус контейнера
docker ps | grep 3xui

# Логи 3xui
docker logs -f 3xui

# Статус Nginx
systemctl status nginx

# Открытые порты
ss -tulpn | grep -E ':(80|443|10000|10007)'
```

### Перезагрузка сервисов:
```bash
# Перезагрузить 3xui
docker-compose -f /opt/3xui/docker-compose.yml restart

# Перезагрузить Nginx
systemctl reload nginx

# Перезагрузить оба
sudo /opt/3xui/maintenance.sh restart
```

### Резервное копирование:
```bash
# Создать резервную копию
sudo /opt/3xui/maintenance.sh backup

# Восстановить из резервной копии
sudo /opt/3xui/maintenance.sh restore /opt/3xui/backups/3xui-backup-20260103_120000.tar.gz

# Полный мониторинг системы
sudo /opt/3xui/maintenance.sh monitor
```

---

## 📱 Получение ссылок для клиентов

### Способ 1: Из веб-панели

1. Откройте https://panel.example.com
2. Авторизуйтесь
3. Перейдите на **Inbounds**
4. Для каждого инбаунда нажмите **Share** или **Export**
5. Скопируйте ссылку (например: `vless://...`, `trojan://...`)

### Способ 2: Через API

```bash
# Список всех инбаундов
curl http://127.0.0.1:54321/api/inbounds

# Получить конфиг в формате Sing-box
curl http://127.0.0.1:54321/api/inbounds/1/export/sing-box
```

### Пример ссылки VLESS REALITY TCP:
```
vless://a1b2c3d4-e5f6-g7h8@your-server-ip:10001?encryption=none&flow=xtls-rprx-vision&sni=www.microsoft.com&type=tcp&security=reality#VLESS-REALITY-TCP
```

---

## 🔧 Администрирование

### Добавление нового пользователя:

1. В веб-панели перейдите на **Users**
2. Нажмите **Add User**
3. Установите срок действия (например, 30 дней)
4. Скопируйте ссылку для пользователя

### Создание дополнительного инбаунда:

```bash
# Используйте REST API
curl -X POST http://127.0.0.1:54321/api/inbounds/add \
  -H "Content-Type: application/json" \
  -d '{
    "protocol": "vless",
    "port": 20001,
    "settings": { ... }
  }'
```

### Экспорт конфигураций:

```bash
# Все инбаунды
docker exec 3xui xray run -config /etc/x-ui/xray.json > xray-config.json
```

---

## 🌍 Использование Cloudflare CDN

### Для WebSocket (/ws):

1. **DNS запись:**
   - Тип: CNAME
   - Имя: panel.example.com
   - Значение: your-server-ip
   - Proxy status: **Proxied** (оранжевое облако)

2. **SSL/TLS режим:** Full

3. **Edge cache TTL:** 30 minutes

4. **Быстрые правила:** Отключить кэширование для /ws

### Для REALITY (не нужен CDN, работает напрямую):
```
Прямое подключение к IP серверу с портом 10001/10002/10003
Cloudflare не поддерживает REALITY
```

---

## 🔄 Автоматизация задач

### Автообновление SSL сертификата:

Добавить в crontab:
```bash
sudo crontab -e

# Добавить строку:
0 2 * * * certbot renew --quiet && systemctl reload nginx
```

### Ежедневное резервное копирование:

```bash
sudo crontab -e

# Добавить строку:
0 3 * * * /opt/3xui/maintenance.sh backup
```

### Еженедельная оптимизация системы:

```bash
sudo crontab -e

# Добавить строку:
0 4 * * 0 /opt/3xui/maintenance.sh optimize
```

---

## 🐛 Troubleshooting

### Проблема: Панель не открывается

**Решение:**
```bash
# Проверить контейнер
docker logs 3xui | tail -50

# Проверить Nginx
nginx -t
tail -f /var/log/nginx/error.log

# Проверить портов
ss -tulpn | grep 8080
```

### Проблема: Клиент не подключается

**Решение:**
```bash
# Проверить открыт ли порт
nc -zv your-server-ip 10001

# Проверить логи Xray
docker exec 3xui tail -50 /var/log/xray/access.log

# Проверить правильность UUID
grep "uuid" /opt/3xui/db/x-ui.db
```

### Проблема: Сертификат не обновляется

**Решение:**
```bash
# Попробовать вручную
sudo certbot renew --force-renewal --quiet

# Проверить логи
tail -f /var/log/certbot.log

# Убедиться, что домен разрешен
nslookup panel.example.com
```

---

## 📊 Мониторинг производительности

### Просмотр статистики:
```bash
sudo /opt/3xui/maintenance.sh stats
```

### Анализ логов Nginx:
```bash
# Самые активные IP
tail -100 /var/log/nginx/3xui_access.log | \
  awk '{print $1}' | sort | uniq -c | sort -rn | head -10

# Частота по дням
tail -1000 /var/log/nginx/3xui_access.log | \
  awk '{print $4}' | cut -d'/' -f1 | sort | uniq -c
```

### Использование ресурсов:
```bash
# CPU и RAM
docker stats 3xui

# Размер БД
du -sh /opt/3xui/db/

# Размер логов
du -sh /var/log/nginx/
```

---

## 📚 Дополнительные ресурсы

- **3x-ui GitHub:** https://github.com/MHSanaei/3x-ui
- **Xray Core:** https://github.com/XTLS/Xray-core
- **Sing-box:** https://sing-box.sagernet.org
- **Clash Meta:** https://github.com/MetaCubeX/mihomo

---

## 🆘 Получение помощи

### Если что-то пошло не так:

1. **Прочитайте логи:**
   ```bash
   docker logs 3xui
   tail -f /var/log/nginx/error.log
   ```

2. **Проверьте конфигурацию:**
   ```bash
   nginx -t
   docker-compose -f /opt/3xui/docker-compose.yml config
   ```

3. **Перезагрузите всё:**
   ```bash
   sudo /opt/3xui/maintenance.sh restart
   ```

4. **Восстановитесь из резервной копии:**
   ```bash
   sudo /opt/3xui/maintenance.sh restore [файл]
   ```

---

## ✅ Чек-лист после установки

- [ ] Панель доступна по https://panel.example.com
- [ ] Изменён пароль администратора
- [ ] SSL сертификат установлен и валиден
- [ ] Все 7 протоколов созданы (если выбраны)
- [ ] Клиент может подключиться хотя бы к одному протоколу
- [ ] Создана первая резервная копия
- [ ] Включено автообновление SSL (cron)
- [ ] Включено ежедневное резервное копирование (cron)
- [ ] UFW переактивирован (если нужен firewall)
- [ ] SSH порт изменён с 22 (опционально)

---

## 📞 Версия

**3X-UI PRO AUTO-INSTALLER v2.0**
- ОС: Ubuntu 22.04 LTS
- Ядро: Xray Core
- Панель: 3x-ui / x-ui-pro
- Дата: 2026-01-03
- Автор: AI Assistant
- Лицензия: MIT (based on upstream projects)

---

## 🎉 Готово!

Если всё прошло успешно, у вас теперь есть полностью функциональный сервер с:
- ✅ 7 протоколами (VLESS REALITY TCP/gRPC/XHTTP, VLESS WS, VMess, Trojan, ShadowSocks)
- ✅ Автоматическим SSL (Let's Encrypt)
- ✅ Nginx reverse proxy со всеми транспортами
- ✅ Web панелью администрирования
- ✅ Инструментами мониторинга и резервного копирования

**Приятного пользования! 🚀**
